import styles from "../styles/Content.module.css";

const SyncPage = () => {
	return (
		<main id={styles.container}>
			<h1>Sync</h1>
			<p>
				This page provide complete database sync (import/export)
				functionalities.
			</p>
		</main>
	);
};

export default SyncPage;
